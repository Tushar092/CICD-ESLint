on : push
jobs :
      eslint :
          runs-on : ubuntu-latest
          steps :
                - uses : actions/checkout@v3
                - run : npx eslint index.js --fix
              
                    
                    